{
  "tasks": {
    "test": "deno test",
    "dev": "deno run --watch --allow-read src/reporting/generateReport.ts",
    "report": "deno run --allow-read src/reporting/generateReport.ts",
    "updateStorageFromApi": "deno run --allow-read --allow-write --allow-net=api.patientaccess.com src/updateRepeatsDataStore.ts"
  },
  "exclude": ["./accessTokenRetrieval"]
}
