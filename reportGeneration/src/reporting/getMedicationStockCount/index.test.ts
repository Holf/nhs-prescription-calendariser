import { assert, describe, it } from "@src/deps.ts";
import { getMedicationStockCount } from "@src/reporting/getMedicationStockCount/index.ts";

describe("getMedicationStockCount", () => {
  it("more stuff", () => {
    //assert(Number.isNaN(getMedicationStockCount("billy")));
  });
});
